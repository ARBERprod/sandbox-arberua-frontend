export const HISTORY_ITEMS_LINE_HEIGHT = 20;
export const HISTORY_CONTAINER_GAP = 16;

// Shown where the backend blanked a nullOnDelete FK (status / delivery / payment method) — see OrderDto.
export const ORDER_VALUE_MISSING = '—';
