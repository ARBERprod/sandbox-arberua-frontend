import { Meta, StoryObj } from '@storybook/react';
import { MainView } from '@/views/MainView';

export default {
  title: 'views/MainView',
  component: MainView,
} as Meta<typeof MainView>;

type Story = StoryObj<typeof MainView>

export const MainViewDefault:Story = {

};
